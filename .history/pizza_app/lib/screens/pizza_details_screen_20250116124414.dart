import 'package:flutter/material.dart';

class PizzaDetailsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Pizza Details')),
      body: Center(child: Text('Pizza Details Screen')),
    );
  }
}
